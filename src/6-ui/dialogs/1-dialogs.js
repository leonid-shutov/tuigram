// Chat ids of the rows currently drawn, in row order. `select` needs to turn a chat id into
// a row index, and SelectOption is opentui's type — it does not carry our chatId back out.
({ chatIds: [] });
