// Width of the dialogs panel, in cells. Also caps how much of a peer name a row can show —
// see 6-ui/dialogs/(common)/Option/from.js.
typeof source.panelWidth === 'number' ? source.panelWidth : 30;
